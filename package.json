{
  "name": "ramp-defi",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ dimap33/ramp-defi",
    "create-local": "graph create --node http://localhost:8020/ dimap33/ramp-defi",
    "remove-local": "graph remove --node http://localhost:8020/ dimap33/ramp-defi",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 dimap33/ramp-defi"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.19.0",
    "@graphprotocol/graph-ts": "0.19.0"
  }
}
